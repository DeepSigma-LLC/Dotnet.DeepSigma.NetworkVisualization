{
  "name": "deepsigma-network-visualization-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "DeepSigma Network Visualization — JS packages and demo workspace",
  "workspaces": [
    "src/js/deepsigma-network-core",
    "src/js/deepsigma-network-react",
    "demo/demo-react"
  ],
  "scripts": {
    "build:libs": "npm run build -w deepsigma-network-core -w deepsigma-network-react",
    "build:demo": "npm run build -w demo-react",
    "build": "npm run build:libs && npm run build:demo",
    "dev:demo": "npm run dev -w demo-react"
  }
}
